package com.eventer.auth.exception.enums;

public enum ErrorCode {
    RESOURCE_NOT_FOUND,
    SERVICE_UNAVAILABLE,
    INTERNAL_SERVICE_ERROR,
    USER_PROFILE_EXISTS,
    INVALID_CREDENTIALS,
    USER_ALREADY_EXISTS;
}
