package com.eventer.userservice.exception.enums;

public enum ErrorCode {
    INVALID_ACCESS_TOKEN,
    INVALID_CREDENTIALS,
    RESOURCE_NOT_FOUND,
    USER_ALREADY_EXISTS,
    UN_AUTHENTICATED,
    INTERNAL_SERVICE_ERROR;
}
